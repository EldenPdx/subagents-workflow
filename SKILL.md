---
name: subagents-workflow
description: "在用户要求使用 Subagents / Multi-Agent、并行智能体、代理委派，或显式调用 $subagents-workflow 完成编码、调查、评审或验证时使用。优先以 Codex 原生 spawn_agent、send_input、wait_agent、close_agent、resume_agent 作为执行机制，并用任务拓扑、边界化契约、依赖波次、上下文控制、恢复、验证和结果汇总规范进行编排；仅在原生能力不可用或用户明确需要跨会话持久编排时考虑外部 Multi-Agent Orchestrator。"
---

# Subagents Workflow

以 **Codex 原生多智能体工具作为执行面**，以成熟 Orchestrator 实践作为编排层。追求最小可行并行度、清晰所有权、低上下文污染和可验证集成。

## 1. 默认原则

1. **原生优先。** 当前环境提供 `spawn_agent`、`send_input`、`wait_agent`、`close_agent`、`resume_agent` 时，直接使用这些工具；不要另起用户任务，不要通过 Shell 启动 `codex exec` Worker，也不要运行外部 Orchestrator 脚本。
2. **父 Agent 负责判断和集成。** 父 Agent 先理解代码与任务、选择拓扑、处理当前关键路径和共享基础，最后审查、整合并验证结果。不要把父 Agent 降级成只转发消息的纯调度器。
3. **只委派真正独立的工作。** 将可并行且不会阻塞父 Agent 下一步的 sidecar 任务交给 Subagents；紧耦合、紧急阻塞或必须立即得到结果的工作留在父 Agent。
4. **最少 Agent 数量。** 一个清晰边界对应一个 Agent。没有独立边界就不创建 Agent；不以“更彻底”为由重复派发同一任务。
5. **写入边界不重叠。** 并行写入型 Agent 必须拥有互斥文件范围。共享基础文件先由父 Agent 完成，或明确交给一个唯一所有者后再扇出。
6. **上下文按需给出。** 传递完成任务所需的最小上下文、决策和文件入口；不要复制整个聊天、整个仓库说明或无关 Skill。
7. **结果必须可验证。** 每个实现 Agent 运行聚焦验证并返回改动文件、验证结果和风险；父 Agent 对集成状态承担最终责任。

## 2. 选择执行拓扑

在首次 `spawn_agent` 前确定拓扑，并记录简短理由：

| 模式 | 适用条件 | 执行方式 |
|---|---|---|
| `direct` | 小型、强耦合、串行任务 | 不创建 Subagent，父 Agent 直接完成 |
| `single_worker` | 一个自包含 sidecar 可与父 Agent 工作并行 | 启动一个 Subagent，父 Agent 继续关键路径 |
| `parallel` | 两个以上互不依赖且写入范围不重叠 | 同一轮启动多个 Subagents |
| `phased` | 存在共享基础或阶段依赖 | 父 Agent 先完成基础；满足启动条件后分波次扇出 |

选择时同时标出：

- 父 Agent 当前立即执行的关键路径；
- 可并行 sidecars；
- 共享基础和唯一所有者；
- 各任务依赖与启动门槛；
- 预期的扇入和验证点。

如果下一步必须依赖某个任务结果，该任务通常应由父 Agent 本地完成，而不是委派后立即等待。

## 3. 父 Agent 的职责

父 Agent 持有全局架构判断和最终责任：

1. 读取任务所需的代码、项目规则和相关 Skills，形成最小任务图。
2. 先处理共享类型、配置、schema、路由、锁文件等会阻塞所有叶子任务的基础工作。
3. 把独立、边界清晰的任务委派出去，同时继续处理不重叠的本地工作。
4. 维护跨 Agent 的已确认决策；影响多个 Agent 时，用 `send_input` 将同一更新发送给所有受影响者。
5. Agent 返回后审查实际改动，不只采信摘要；整合结果并运行必要的跨模块验证。
6. 关闭不再需要的 Agent，释放并发槽位。

不要重复实现已委派的同一子任务。父 Agent 可以完成不同的关键路径工作、共享基础、集成修复和最终验证。

## 4. 编写边界化任务契约

每个 `spawn_agent` prompt 必须是自包含任务契约，至少包含：

```text
角色：<implementation | investigation | review | verification>
目标：<一个可独立完成的结果>
限定任务：<具体要做什么>
范围外事项：<不要顺手扩展的内容>
读取入口：<先读的文件、符号、测试或文档>
允许写入：<唯一文件或目录范围；只读任务写“无”>
禁止写入：<共享文件、其他 Agent 范围、生成物等>
依赖和启动条件：<无，或依赖哪个已完成结果>
已确认决策：<接口、约束、兼容性与用户要求>
预期产物：<代码、测试、分析或评审结论>
验证命令：<聚焦且可运行；没有则说明原因>
完成汇报：<状态、摘要、改动文件、验证、风险>
```

遵循以下发送规则：

- 用实际值替换所有占位符。
- 代码 Agent 直接在自己的 forked workspace 中修改文件，并在最终回复列出完整文件路径。
- 调查、评审和验证 Agent 默认只读；只有契约明确授权时才修改文件。
- 将用户指定 Skill 的名称和路径只分配给真正需要它的 Agent；要求该 Agent 先读取对应 `SKILL.md`。
- 默认使用 `fork_context: false` 并给出自包含契约；只有任务依赖大量无法简洁重述的当前会话细节时才使用 `fork_context: true`。
- 省略模型和 reasoning override；只有用户明确要求特定模型时才覆盖。

## 5. 扇出与依赖波次

1. 在一个调度轮次中启动所有当前可运行、互不重叠的任务，减少串行等待。
2. 为每个 Agent 指派不同目标；不要让多个 Agent竞争性实现同一方案。
3. 写入型任务只有在文件、生成物、端口、数据库和锁资源互不冲突时并行。
4. 只读调查和独立评审可更积极地并行。
5. `phased` 模式中，前置阶段完成并形成稳定接口后再启动依赖叶子；不要让 Agent 猜测尚未确定的共享基础。
6. 某个实现单元完成后，可以立即启动其独立验证，同时其他实现继续运行。
7. 扇入前确认所有必需任务已完成或明确阻塞，且没有未归属或范围重叠的改动。

启动 Subagents 后立即继续父 Agent 的非重叠工作。只有下一步确实依赖 Agent 结果时才等待。

## 6. 原生工具协作协议

### `spawn_agent`

- 用于新的、独立且物质性推进主任务的子任务。
- 优先一次并行启动多个互不依赖的 Agent。
- coding 任务要求 Agent 在其 forked workspace 直接修改，并列出改动路径。
- 不把紧急阻塞工作、模糊大任务或父 Agent 下一步立即依赖的工作交出去。

### `send_input`

- 用于补充已确认决策、解除依赖、纠正范围偏移或要求聚焦修复。
- 同一未解决任务优先复用现有 Agent；不要用新的 Agent 重复覆盖它。
- 需要立即改向时使用中断；否则排队发送。

### `wait_agent`

- 仅在下一步被结果阻塞时调用。
- 可以一次等待多个目标；使用有界但足够长的等待时间。
- 不忙轮询，不因没有立即完成而重复等待；等待期间优先完成其他本地工作。

### `close_agent` / `resume_agent`

- 结果已集成或 Agent 不再需要时立即关闭。
- 需要继续同一上下文时恢复已有 Agent，而不是创建重复 Agent。
- 完成状态的 Agent 在关闭前仍占用并发容量，因此及时清理。

## 7. 上下文与决策管理

将 Orchestrator 的文件协议压缩为原生会话内的最小控制面：

- **任务图**：角色、目标、依赖、写入范围、状态。
- **决策表**：只记录会影响多个 Agent 的稳定决策。
- **Agent 摘要**：状态、产物、验证和风险。

普通任务在父 Agent 的计划与会话上下文中维护这些信息，不强制创建分支、Worktree 或工作流文档。只有任务长到状态可能丢失、用户要求审计记录，或仓库已有规定时，才创建轻量持久文档。

Agent 之间不直接依赖隐式消息。由父 Agent 中央汇总决策并显式转发；不要假设一个 Agent 看到了另一个 Agent 的对话或改动。

## 8. 健康检查与恢复

使用事件驱动的检查，而不是固定频率轮询：

- Agent 返回完成或阻塞；
- 父 Agent 即将进入依赖步骤；
- 发现范围冲突、接口变化或验证失败；
- 用户提供了影响任务的新信息。

按以下恢复阶梯处理问题：

1. **澄清**：用 `send_input` 给出缺失事实、失败输出或更窄的完成条件。
2. **纠偏**：明确停止越界方向，保留有效成果，要求聚焦剩余任务。
3. **替换**：Agent 无法恢复时关闭它，并把经过收窄、带有已知事实的任务交给新 Agent。
4. **升级**：只有涉及产品决策、破坏性操作、凭据或无法从现有材料判断的外部事实时才询问用户。

不要为同一阻塞连续创建多个 Agent。替换前先确认原 Agent 无法通过一次明确纠偏恢复。

## 9. 验证、评审与集成

每个写入型 Agent 完成前：

- 检查实际修改未超出允许范围；
- 运行契约中的聚焦验证；
- 报告未运行的验证及原因；
- 列出改动文件和残余风险。

父 Agent 扇入时：

1. 审查上传的改动和实际 diff，确认接口、风格和用户要求一致。
2. 集成不重叠成果；共享文件与语义冲突由父 Agent 解决。
3. 在可并行时启动独立 verification/review Agent，对关键风险、测试或跨模块契约做第二视角检查；该 Agent 不重复实现功能。
4. 运行覆盖集成面的测试、类型检查、构建或格式验证。
5. 对失败范围做聚焦修复，并只重跑受影响验证。
6. 确认没有意外文件、未归属改动、遗漏依赖或未经验证的完成声明。
7. 关闭全部完成或放弃的 Agent。

最终完成条件是父 Agent 对集成后的真实工作区验证通过，而不是所有 Subagents 都返回 `completed`。

## 10. 完成汇报格式

要求每个 Subagent 最终回复保持简短并包含：

```text
状态：completed | blocked
角色与目标：...
主要结果：...
改动文件：... | 无
验证：<命令与结果> | 未运行及原因
风险/阻塞：... | 无
建议的集成动作：...
```

父 Agent 最终向用户汇总：

- 使用的拓扑以及实际 Agent 数量和角色；
- 父 Agent 完成的关键路径与共享基础；
- 各 Agent 的主要结果和改动范围；
- 集成验证结果；
- 未覆盖项、残余风险或阻塞原因。

不要转储完整 Agent 对话、长日志或未经核验的自我报告。

## 11. 外部 Orchestrator 的回退边界

保持 Codex 原生工具为默认执行机制。只有出现以下情况之一时，才考虑切换到 `$multi-agent-orchestrator`：

- 当前环境没有原生多智能体工具；
- 用户明确要求独立 CLI、异构模型或显式 Git Worktree；
- 任务需要跨会话、数小时后台运行、落盘审计、自动超时重启或进程级恢复；
- 用户明确指定使用 Multi-Agent Orchestrator。

切换前说明原因和代价，并取得用户明确同意或显式调用。不要在同一写入范围同时运行原生 Subagents 与外部 CLI Workers。允许按阶段混合：原生 Agent 做规划/评审，外部 Orchestrator 做经用户批准的持久实现，随后回到原生会话集成。

## 12. 反模式

避免以下行为：

- 父 Agent 不理解任务就把规划全部外包；
- 为强耦合或很小的任务强行创建 Agent；
- 把当前关键路径交出去后立即空等；
- 并行 Agent 修改同一文件、锁文件、schema 或生成物；
- 用多个 Agent 重复解决同一个未收敛问题；
- 将整个聊天或整个仓库内容复制给每个 Agent；
- 只看摘要、不审查实际改动；
- 小任务强制创建分支、Worktree、提交和大量协调文档；
- 原生能力可用时仍通过 `codex exec` 模拟 Subagents；
- 未经用户同意切换到绕过审批或沙箱的外部 Worker。

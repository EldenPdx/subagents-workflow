interface:
  display_name: "Subagents Workflow"
  short_description: "原生 Subagents 的任务拆分、并行协作、恢复与集成验收"
  default_prompt: "Use $subagents-workflow to complete this task with bounded native subagents, non-overlapping ownership, and integrated validation."

policy:
  allow_implicit_invocation: true
